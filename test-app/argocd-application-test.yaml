apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: go-monitoring-app-test
  namespace: argocd-minimal  # Angepasst für den Test-Cluster
spec:
  project: default
  source:
    repoURL: 'https://github.com/Marmor1243/argocd-learning.git'
    targetRevision: HEAD
    path: kubernetes
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
